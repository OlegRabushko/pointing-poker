import { ScoreCardDiv } from './StyledScoreCard';

const ScoreCard = () => {
  return (
    <ScoreCardDiv className="user-card">
      <span className="score-card__name">Unknown</span>
    </ScoreCardDiv>
  );
};

export default ScoreCard;
