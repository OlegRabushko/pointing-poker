import { StyledScoreCard } from './score-card.styled';

const ScoreCard = () => {
  return (
    <StyledScoreCard className="user-card">
      <span className="score-card-name">Unknown</span>
    </StyledScoreCard>
  );
};

export default ScoreCard;
